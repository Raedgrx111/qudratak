# دليل نشر منصة قدراتك على الإنترنت

المشروع جاهز للنشر المباشر من هذا المستودع. الطريقة الموصى بها: منصة **Railway** (تربط بمستودع GitHub وتنشئ رابطًا عامًا تلقائيًا، وتحفظ قاعدة البيانات على قرص دائم).

---

## أولاً: النشر على Railway (الأسهل)

### 1) أنشئ المشروع
1. افتح [railway.app](https://railway.app) وسجّل الدخول بحساب GitHub.
2. اضغط **New Project** ← **Deploy from GitHub repo** ← اختر `Raedgrx111/qudratak`.
3. انتظر انتهاء البناء (سيُكتشف Next.js تلقائيًا).

### 2) أضف متغيرات البيئة
من تبويب **Variables** أضف:

| المتغير | القيمة | ملاحظة |
|---|---|---|
| `DATABASE_URL` | `file:/app/db/custom.db` | مسار قاعدة البيانات داخل القرص الدائم |
| `JWT_SECRET` | قيمة عشوائية طويلة | ولّدها بأمر `openssl rand -base64 32` |
| `RESEND_API_KEY` | `re_...` | من لوحة resend.com/api-keys |
| `MAIL_FROM` | بريد من نطاقك الموثق | مثال: `no-reply@qudratak.com` — إن لم توثق نطاقًا بعد، اتركه فارغًا وسيعمل التوثيق اليدوي من لوحة المالك |

### 3) أضف قرصًا دائمًا لقاعدة البيانات
1. من خدمة المشروع ← تبويب **Settings** ← **Volumes** ← **New Volume**.
2. اربطه بالمسار `/app/db` — هذه الخطوة تضمن عدم ضياع بيانات الطلاب عند إعادة النشر.

### 4) أنشئ الرابط العام
1. تبويب **Settings** ← **Networking** ← **Generate Domain**.
2. ستحصل على رابط مثل: `https://qudratak-production.up.railway.app` — هذا هو رابط موقعك العام.

### 5) أنشئ حساب المالك (مرة واحدة فقط)
من تبويب المتغيرات أضف مؤقتًا:
- `OWNER_EMAIL` = بريدك الحقيقي
- `OWNER_NAME` = اسمك
- `OWNER_PASS` = كلمة مرور قوية (٨ أحرف فأكثر)

ثم من تبويب **Deployments** افتح سجل البناء وشغّل أمرًا مؤقتًا، أو استخدم Railway Shell:
```bash
npm run seed:owner
```
بعدها احذف `OWNER_PASS` من المتغيرات.

### 6) فعّل إرسال أكواد التحقق للطلاب
حد Resend التجريبي يرسل لبريدك أنت فقط. لإرسال الأكواد لبريد الطلاب:
1. أضف نطاقك في [resend.com/domains](https://resend.com/domains) وأكمل التوثيق (سجلات DNS).
2. غيّر `MAIL_FROM` إلى بريد من نطاقك الموثق، مثل `no-reply@نطاقك.com`.
3. أعد النشر. من هذه اللحظة يصل رمز التحقق لبريد أي طالب تلقائيًا.

---

## ثانيًا: بديل VPS (لمن يريد تحكمًا كاملًا)

على خادم Ubuntu مثبَّت عليه Node 20+:

```bash
git clone https://github.com/Raedgrx111/qudratak.git && cd qudratak
npm install
# أنشئ ملف .env بنفس المتغيرات أعلاه (مع DATABASE_URL=file:/var/data/qudratak.db)
npm run build
npm run seed:owner
npx pm2 start npm --name qudratak -- start
```

ثم وجّه Nginx/Caddy إلى المنفذ `3000` وأصدر شهادة SSL مجانية:
```bash
caddy reverse-proxy --from your-domain.com --to localhost:3000
```

---

## استكشاف الأخطاء بعد النشر

| المشكلة | الحل |
|---|---|
| الصفحة تفتح لكن الدخول لا يعمل | تأكد من ضبط `JWT_SECRET` وإعادة النشر |
| «الإرسال البريدي غير مفعّل» | لم يُضبط `MAIL_FROM` بنطاق موثق — استخدم التوثيق اليدوي من لوحة المالك مؤقتًا |
| خطأ قاعدة البيانات عند أول تشغيل | تأكد من ربط القرص الدائم بمسار `/app/db` ومن صحة `DATABASE_URL` |
| أرقام سجل البناء | سجل البناء يطبع الرمز البريدي تلقائيًا عند عمل وضع الإرسال اليدوي — هذا مقصود وآمن (الرمز محفوظ بصمةً ولا يُخزَّن نصًا) |
